% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funs.R
\name{upload_media}
\alias{upload_media}
\title{Upload Image to WordPress Media Library}
\usage{
upload_media(image_path, wp_site, wp_header)
}
\arguments{
\item{image_path}{Character. The file path of the image to be uploaded (e.g., "path/to/image.jpg").}

\item{wp_site}{Character. The base URL of the WordPress site (e.g., "https://example.com").}

\item{wp_header}{List. A named list created using \code{add_headers(Authorization = paste("Basic", wp_token))} for authentication, where \code{wp_token} is the base64-encoded username and password for the WordPress site.}
}
\value{
A list containing the details of the uploaded media, or an error message if the upload fails.
}
\description{
Uploads an image from the local drive to the WordPress media library using the REST API.
}
\details{
This function allows users to upload images directly to the WordPress media library via the REST API. The \code{wp_header} parameter must be passed using \code{add_headers(Authorization = paste("Basic", wp_token))}, where \code{wp_token} is the base64-encoded string of your username and password. The \code{image_path} should point to a valid image file on the local drive.
}
\examples{
\dontrun{
# Upload an image to the media library
wp_header <- httr::add_headers(Authorization = paste("Basic", wp_token))
upload_media(image_path = "path/to/image.jpg", wp_site = "https://example.com", wp_header = wp_header)
}

}
