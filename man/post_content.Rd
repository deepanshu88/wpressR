% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funs.R
\name{post_content}
\alias{post_content}
\title{Post Content to WordPress}
\usage{
post_content(wp_site, wp_header, data, content_type = "posts")
}
\arguments{
\item{wp_site}{Character. The base URL of the WordPress site (e.g., "https://example.com").}

\item{wp_header}{List. A named list created using \code{add_headers(Authorization = paste("Basic", wp_token))} for authentication, where \code{wp_token} is the base64-encoded username and password for the WordPress site.}

\item{data}{List. A list containing the content data to be posted. The list must include required fields such as \code{title} and \code{content}.}

\item{content_type}{Character. The type of content to create, either "posts" (default) or "pages".}
}
\value{
A list containing the details of the newly created content, or an error message if the request fails.
}
\description{
Creates a new post or page on a WordPress site using the REST API.
}
\details{
This function allows the creation of new posts or pages on a WordPress site via the REST API. The \code{wp_header} parameter must be passed using \code{add_headers(Authorization = paste("Basic", wp_token))}, where \code{wp_token} is the base64-encoded string of your username and password. The \code{data} list should contain all necessary fields like \code{title}, \code{content}, and any other post attributes.
}
\examples{
\dontrun{
# Post a new blog post
wp_header <- httr::add_headers(Authorization = paste("Basic", wp_token))
data <- list(title = "New Blog Post", content = "This is the content of the post.")
post_content(wp_site = "https://example.com", wp_header = wp_header, data = data)

# Post a new page
post_content(wp_site = "https://example.com", wp_header = wp_header, data = data, content_type = "pages")
}

}
